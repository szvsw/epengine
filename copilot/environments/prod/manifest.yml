# The manifest for the "prod" environment.
# Read the full specification for the "Environment" type at:
#  https://aws.github.io/copilot-cli/docs/manifest/environment/

# Your environment name will be used in naming your resources like VPC, cluster, etc.
name: prod
type: Environment

# Import your own VPC and subnets or configure how they should be created.
network:
  vpc:
    id: '' # masked id 
    subnets:
      private: # masked private id 
        - id: ''
        - id: ''

# Configure the load balancers in your environment, once created.
# Configuring internal load balancer instead.
http:
  private: # masked private subnets id 
    subnets: ['', ''] 
    ingress:
      vpc: true  # Enable incoming traffic within the VPC to the internal load balancer.

# http:
  # public:
    # certificates: [${CERTIFICATE_ARN}]

# Configure observability for your environment resources.
observability:
  container_insights: false